{# def d, files #}

{% extends "oarepo_ui/detail.html" %}

{%- block head_links %}
{{ super() }}
{%- endblock %}

{% block record_header %}
    <h1 class="ui header" style="padding-top: 2em; padding-left: 2em;">
        <i class="flask icon"></i>
        <div class="content">
            {{ d.metadata.name }}
            <div class="sub header">{{ d.metadata.description }}</div>
        </div>
    </h1>
{% endblock %}

{% block record_main_content %}
    <ExperimentsDetailRoot d={d} />
{% endblock %}

{% block record_sidebar %}
    <ExperimentsSideBar d={d} files={files} />
{% endblock record_sidebar %}
